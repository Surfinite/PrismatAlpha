# PrismataAI training dependencies
# Install: pip install -r training/requirements.txt
#
# PyTorch with XPU support (Intel Arc GPU):
#   pip install torch==2.10.0+xpu --index-url https://download.pytorch.org/whl/xpu
# PyTorch CPU-only:
#   pip install torch==2.10.0 --index-url https://download.pytorch.org/whl/cpu

numpy>=2.0
tqdm>=4.60
