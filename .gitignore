# ===========================================
# PrismataAI .gitignore
# ===========================================

# --- Visual Studio build artifacts ---
visualstudio/Release/
visualstudio/Debug/
visualstudio/Static Release/
visualstudio/*.sdf
visualstudio/*.suo
visualstudio/*.opensdf
visualstudio/*.psess
visualstudio/*.vsp
visualstudio/*.vspx
visualstudio/.vs/
visualstudio/Prismata_GUI/

# --- Build outputs (bin/) ---
bin/*.exe
bin/*_d.exe
bin/*.pdb
bin/*.ilk
bin/*.iobj
bin/*.ipdb
bin/*.bsc
bin/*.bmp
bin/*.dat
bin/*.swc
bin/*.swf
bin/__ab.txt
bin/__uct.txt
bin/mcds_error_log.txt
bin/tests/
bin/qtdeploy/
bin/html/
bin/graphviz/
bin/debug_state.txt
bin/NUL

# --- Replays (large, generated) ---
bin/asset/replays/

# --- Neural net weights: NOW TRACKED (2026-06-01). The swap-in AI path is sanctioned and
#     these models are small (~0.7 MB each) and expensive to reproduce, so we version them.
#     Large .pt source checkpoints (training/models/, below) stay untracked -- D: + daily IDrive.

# --- Self-play binary output in bin/ (large, generated) ---
bin/training/

# --- Training data & models (2GB+, regenerated) ---
training/data/*.h5
training/data/*.jsonl
training/data/*.pt
training/data/splits/
training/data/masterbot_fleet*/
training/data/new_fleet*/
training/data/raw_states*
training/data/train.pt
training/data/val.pt
training/data_test/
training/models/
training/models_test/
training/runs/
training/__pycache__/

# --- Opening book analysis outputs (regenerated from scripts) ---
training/data/pair_opening_analysis.json
training/data/triple_opening_analysis.json
training/data/tech_timing.json
training/data/unit_opening_impact.json
training/data/universal_openings.json
training/data/opening_book_*/

# --- Build/pipeline logs ---
build_output.txt
build_stderr.txt
pipeline_log.txt
pipeline_stderr.txt
stderr.txt

# --- Temp batch scripts ---
build_and_run.bat
compile_test.bat
do_rebuild.bat
rebuild.bat
run_pipeline.bat

# --- Test output in bin/ ---
bin/test_output.txt
bin/test_stderr.txt

# --- Claude Code local settings ---
.claude/
.claude.local.md

# --- Windows artifacts ---
NUL
Thumbs.db
Desktop.ini

# --- Old context files (moved to docs/session-logs/) ---
CLAUDE_*.md

# --- Garbled filename artifact (moved to docs/artifacts/ for human review) ---
docs/artifacts/

# --- Object files (Linux/GCC builds) ---
source/*.o
source/main/*.o
source/benchmark/*.o
source/json/*.o

# --- IDE / editor ---
.vscode/
.idea/
*.swp
*.swo
*~
*.user

# --- Python environments & packaging ---
venv/
.venv/
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/

# --- Garbled test output filename (UTF-8 artifact) ---
c\357\200\272*

# --- IDE workspace files ---
*.code-workspace

# --- Run logs (generated) ---
bin/selfplay_10k_log.txt
bin/threadtest_output.txt
chroma.log

# --- Validation output (generated) ---
bin/validation_*
bin/asset/config/validation_*

# --- Synthetic training data (generated) ---
training/data/selfplay_synthetic/

# --- Doc backups (local) ---
docs/backup_claude_md_2026-02-14/

# --- GCP credentials & temp files ---
gcp/.aws_credentials
gcp/.ec2_config_check.txt
gcp/.gcp_config_check.txt

# --- Azure credentials & debug scripts with secrets ---
azure/.aws_credentials
azure/.startup_debug.ps1

# --- Watcher runtime state (changes every 5 min) ---
aws/watcher_log.txt
aws/watcher_status.json

# --- AWS temp/test files ---
aws/.log_stdout_spot.txt
aws/.s3_config.txt
aws/.test_shard.bin

# --- Decompiled/decompressed SWF (large, not source) ---
prismata_decompiled/
prismata_decompressed.swf

# --- Run output logs ---
bin/selfplay_log.txt
bin/tournament_log.txt
bin/tournament_eval_log*.txt
bin/prismata_capture.log
training/exp*.log

# --- Junk output dir ---
--output

# --- Dashboard ---
dashboard/node_modules/

# --- Temporary eval directories (contain binaries + replays) ---
bin_eval_*/

# --- Playwright MCP artifacts ---
.playwright-mcp/

# --- Old training scripts (superseded, kept locally as reference) ---
OLD_training/

# --- Root-level matchup error logs ---
matchup_err*.log

# --- Other ---
*.zip
*.stackdump
emscripten/*.js
emscripten/*.data
emscripten/*.mem
qt/build-PrismataAI-*

# --- Temp PowerShell scripts, scratch files, and temp directories in root ---
tmp_*.ps1
tmp_*.json
tmp_*.js
tmp/
tmp_*/

# --- bin/ generated/debug/log files ---
bin/*.log
bin/*.txt
bin/*.json
bin/replays_test/

# (neural_weights_*.bin are now TRACKED -- see the weights note near the top of this file)

# --- Dashboard screenshots and logs ---
dashboard/logs/
dashboard/*.png
dashboard/*.md
dashboard_*.png

# --- GCP temp/debug dot-scripts and output files ---
gcp/.*
gcp/*.log
gcp/*.txt

# --- AWS log copies ---
aws/log_copy/

# --- Training run logs ---
training/*.log

# --- docs/wiki binary files ---
docs/wiki/*.pdf

# --- Python cache in tools/ ---
tools/__pycache__/

# --- Discord extraction working files (large, regenerated) ---
tools/discord_extraction/chunks/
tools/discord_extraction/batch_status.json
tools/discord_extraction/processed_chunks.json
tools/discord_extraction/processed_chunks_extractions_sonnet.json

# --- SWF binary extraction temp files ---
tmp_swf_extract/

# --- Temp stdout/stderr captures ---
tmp_stdout.txt
tmp_stderr.txt

# --- Tools directory (sniffer, advisor, stats — local only) ---
tools/
# Exception: Godot 3D viewer preprocessor tools
!tools/card_id_map.js
!tools/position_calculator.js
!tools/snapshot_schema.js
!tools/replay_to_snapshots.js
!tools/extract_viewer_assets.js
!tools/audit_visual_fidelity.js
!tools/visual_state.js
!tools/status_overlay.js
# Opening Book Explorer
!tools/ob_explorer.py
!tools/ob_explorer_data.py
!tools/ob_explorer_viz.py
!tools/requirements-explorer.txt
!tools/tests/

# --- Cloud config (resource IDs, project names) ---
cloud-config.env

# --- Generated results directories ---
eval-results/
matchup-results/
replays/
validation_results/

# --- js_engine generated data, logs, and temp files ---
js_engine/_suggest_state_*.json
js_engine/*.jsonl
js_engine/*.log
js_engine/*.txt
js_engine/all_codes_*.json
js_engine/prepatch_*.json
js_engine/cloud_results/
js_engine/matchup_replays/
js_engine/overnight_run/
js_engine/stuck_replays/
js_engine/validation_results.json
js_engine/test_movejson.json
js_engine/test_suggest_state.json

# --- Training data directories (large, generated) ---
training/data_*/
training_*.log

# --- Misc generated files in bin/ ---
bin/eval_history.csv
bin/test_dump.jsonl

# --- Batch launcher (personal config) ---
run_prismata_tools.bat

# --- Python cache (anywhere in tree) ---
__pycache__/
*.pyc

# --- CMake build directory ---
build/

# --- Cloud archive (large local snapshot) ---
gcp-archive/

# --- Superpowers brainstorm cache ---
.superpowers/

# --- Explicit "ignore me" sandbox dirs ---
**/IGNORE/

# --- Bot/replay-parser/training local artifacts ---
# deadgame/ source moved to <ladder> repo; ignore any local runtime leftovers
deadgame/
bin/asset/images/icons/extracted_hd/
bin/test_*/
js_engine/_suggest_state.json
js_engine/test_replays/
training/cloud-runs/
training/data/test_*
training/data/dataset_validation.json

# --- Root-level scratch artifacts ---
*.log
audit_batch_*.json
cross_validation_report_*.json
live_test_data*.json
ob_entries.json
ob_report.json
replay_stats.tar.gz
replay_stats/
buy-panel-*.png
viewer-*.png

# --- AWS scratch ---
aws/console_out.json
aws/test_steam_input*.json
aws/tmp_matchup*.log

# --- Zen mode: keep sources/scripts, never the built ~19MB game SWF (rebuild via apply_zen_patches.ps1) ---
docs/scratch/zen_mode/*.swf
